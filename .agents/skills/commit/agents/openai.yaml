interface:
  display_name: "Commit Changes"
  short_description: "Build and create scoped conventional commits"
  default_prompt: "Use $commit to split and commit the current changes by purpose."

policy:
  allow_implicit_invocation: false
