---
description: Stage and commit changes by feature using conventional commits
---

Read `.agents/skills/commit/references/workflow.md` completely and follow it as
the required commit procedure.

The user invoked `/commit`, so this invocation supplies the explicit permission
required by the workflow. Commit only the requested working-tree changes and
never push.
