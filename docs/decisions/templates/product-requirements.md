# Product Requirements: [Change name]

Technical Design: [Change name](YYYY-MM-DD-<slug>.design.md)

<!-- Product part of a Split Change Proposal, paired with Technical Design.
     Save as docs/decisions/YYYY-MM-DD-<slug>.requirements.md, with the same date and slug as the
     .design.md; the date is the day the draft starts and takes the place of a Created field.
     The link above is required: it points at the actual Technical Design file, which links back.
     Both documents together are the proposal. There is no third summary or proposal file.
     Make the product case reviewable without implementation details.
     Keep the authoritative R IDs here; Technical Design references them.
     Delete sections with nothing to say; keep the section names and their order, which
     DecisionDocsTests checks. Reference other proposals by filename, never by folder path.
     Revise the draft while the change is open and preserve meaningful reversals and their reasons
     in Product Decisions. After the change's last PR merges, the only later write is a
     "Superseded in part by <doc>" note that a reversing PR appends in a blockquote below the title.
     Full explanation and the separate review questions, upstream:
     https://github.com/josephjang/change-proposal/blob/main/docs/split-proposals.md
     (this template follows the upstream template at revision 8fba2af). -->

## Summary

<!-- The proposed outcome and why it matters, readable on its own. -->

## Problem

<!-- What is wrong now, for whom, with evidence. Describe the user's experience rather
     than services or file names. A system change can describe its consumers' experience. -->

## Goals

<!-- End conditions the change makes possible. -->

## Non-Goals

<!-- What is deliberately outside this change and why. -->

## Requirements

<!-- R1, R2, ...: observable acceptance criteria and the proposal's stopping condition.
     Include necessary compatibility or performance promises here.
     Keep IDs stable across revisions. Technical Design links each to Design and Test Strategy;
     Verification records the actual results of those checks. -->

- R1: [Observable acceptance criterion.]
- R2: [Observable acceptance criterion.]

## Product Decisions

<!-- Record product choices with alternatives. Link technical reasoning where it affects
     a product promise; each rationale has one authoritative home.
     Use PD IDs for new decisions; retain existing IDs when splitting a draft.
     Revisit the affected product judgment when a technical discovery changes the promise. -->

- **PD1: [Decision sentence.]** [Rejected alternative, reason, and when to revisit.]

## Risks

<!-- Accepted product risks and limitations, with the reason they are acceptable.
     Link the technical cause or mitigation in Technical Design when relevant. -->
