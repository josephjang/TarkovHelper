# Change Proposal: [Change name]

<!-- Records the intent and judgment behind this change, and is what the change is built from, by a
     person or an AI assistant: started before the work, finished with it, kept as the record after.
     Committed in the same pull request as the change, at docs/decisions/YYYY-MM-DD-<slug>.md; the
     date is the day the draft starts and takes the place of a Created field.
     Delete any section with nothing to say; keep the section names and their order, which
     DecisionDocsTests checks. If technical complexity shows up mid-flight, split: rename this file
     to .requirements.md, retitle it Product Requirements, and add the matching .design.md.
     Reference code by symbol (UserDataDbService.InitializeAsync, line numbers optional) and other
     proposals by filename, never by folder path; never paste schemas, signatures or file lists.
     Revise the draft while the change is open and keep a reversed decision under Decisions. After
     the change's last PR merges, the only later write is a "Superseded in part by <doc>" note that a
     reversing PR appends in a blockquote below this title.
     What each section is for, and why, is explained once, upstream:
     https://github.com/josephjang/change-proposal/blob/main/docs/guide.md
     (this template follows the upstream template at revision 8fba2af). -->

## Summary

<!-- Optional. Three to five sentences: what changes, why, what does not change.
     Delete when the proposal is short enough to speak for itself. -->

## Problem

<!-- What is wrong right now, for whom, with the evidence (issue, metric, request).
     How it works today and why it was built that way, when that matters: the diff shows only "after".
     "What is the problem", not "what we will build". -->

## Goals

<!-- End states: "X is possible", not "build X". -->

-

## Non-Goals

<!-- What this change deliberately does not do, and why, decided at scoping or discovered
     during the work. -->

-

## Requirements

<!-- Statements judgeable true or false, the stopping condition: when every one is true,
     the work is done. -->

- R1:
- R2:
- (optional) Metric: (name) baseline (value) → target (value), (how measured)

## Decisions

<!-- Only decisions that had alternatives. If none, delete this section.
     One bullet per decision: an id and the decision sentence in bold, then the rejected
     alternative, a reason that would change if the facts changed, and when to revisit, as prose. -->

- **D1: [Decision sentence.]** [Rejected alternative, reason, and when to revisit.]

## Risks

<!-- Trade-offs accepted knowingly, with the reason. -->

-
