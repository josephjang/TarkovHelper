# Repository Agent Instructions

- Before doing repository work, read and follow `CLAUDE.md` as shared
  repository guidance.
- Before changing files under `TarkovHelper/`, also read and follow
  `TarkovHelper/CLAUDE.md`.
- Before changing files under `TarkovDBEditor/`, also read and follow
  `TarkovDBEditor/CLAUDE.md`.
- More specific project guidance overrides repository-wide guidance when the
  instructions conflict.
- Although the referenced files are named `CLAUDE.md`, treat their repository,
  architecture, build, test, documentation, Git, and release rules as
  agent-independent.
