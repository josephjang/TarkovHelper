<Window x:Class="TarkovHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TarkovHelper"
        mc:Ignorable="d"
        Title="Tarkov Helper" Height="800" Width="1400"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        SizeChanged="Window_SizeChanged">

    <Window.Resources>
        <!-- Separator scaffold shared by every stacked Settings-overlay section -->
        <Style x:Key="SettingsSectionBorderStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Margin" Value="0,12,0,0"/>
            <Setter Property="Padding" Value="0,16,0,0"/>
        </Style>

        <!-- Exact-one profile selector. The 16 px marker slot is always reserved so
             the brief log-transition signal never changes the segment widths. -->
        <Style x:Key="GameModeRadioStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Grid Width="16" Height="16" Margin="0,0,5,0"
                                          VerticalAlignment="Center">
                                        <Path x:Name="SelectedCheck"
                                              Data="M 2,8 L 6,12 L 14,3"
                                              Stroke="{StaticResource AccentBrush}"
                                              StrokeThickness="2.4" StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" Stretch="Uniform"
                                              Visibility="Hidden"/>
                                        <Path x:Name="AutomaticBolt" Width="13" Height="15"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Data="M 9,1 L 3,9 L 7,9 L 5,15 L 13,6 L 9,6 Z"
                                              Fill="{StaticResource AccentBrush}"
                                              Stretch="Uniform" Visibility="Hidden"/>
                                    </Grid>
                                    <TextBlock Text="{TemplateBinding Content}"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"
                                               FontWeight="{TemplateBinding FontWeight}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </StackPanel>
                                <!-- Focus ring drawn as an overlay inside the existing border, so
                                     showing it costs no layout space. Growing the border itself
                                     would widen the focused segment and shift its siblings. -->
                                <Border x:Name="focusRing" Margin="-2"
                                        BorderBrush="{StaticResource AccentHoverBrush}"
                                        BorderThickness="1" CornerRadius="2"
                                        IsHitTestVisible="False" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="SelectedCheck" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="focusRing" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <!-- There is deliberately no lasting "AutoSelected" marker state.
                                 PRD R6: the selector does not persist Manual/Auto/Pinned source
                                 state; an automatic change gets a brief cue and an accessible
                                 announcement, then the selector returns to its neutral look. -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="Tag" Value="AutomaticCue"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="SelectedCheck" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="AutomaticBolt" Property="Visibility" Value="Visible"/>
                                <!-- FillBehavior=Stop and the paired RemoveStoryboard are both
                                     required: AutoReverse with RepeatBehavior="2x" ends on the
                                     From value (0.35), so a held animation would pin the bolt
                                     at 35% opacity for the rest of the session, and an
                                     EnterActions clock is not released when the trigger exits. -->
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard x:Name="AutomaticCuePulse">
                                        <Storyboard AutoReverse="True" RepeatBehavior="2x"
                                                    FillBehavior="Stop">
                                            <DoubleAnimation Storyboard.TargetName="AutomaticBolt"
                                                             Storyboard.TargetProperty="Opacity"
                                                             From="0.35" To="1" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <RemoveStoryboard BeginStoryboardName="AutomaticCuePulse"/>
                                </MultiTrigger.ExitActions>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The compact popup is a visual continuation of its trigger, not a system menu. -->
        <Style x:Key="ActiveProfileContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveProfileMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="{DynamicResource AppFont}"/>
            <Setter Property="FontSize" Value="{DynamicResource FontSizeXSmall}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="itemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Path x:Name="SelectedCheck" Width="16" Height="16"
                                      HorizontalAlignment="Left" VerticalAlignment="Center"
                                      Data="M 2,8 L 6,12 L 14,3"
                                      Stroke="{StaticResource AccentBrush}"
                                      StrokeThickness="2.4" StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round" Stretch="Uniform"
                                      Visibility="Hidden"/>
                                <!-- FontFamily is deliberately not forwarded: it is an
                                     inheritable property, so this TextBlock already picks up
                                     the MenuItem's AppFont from the Setter above, and the
                                     font-policy test (FontAssetsTests) allows only the three
                                     approved family literals in XAML. -->
                                <TextBlock Grid.Column="1" Text="{TemplateBinding Header}"
                                           Margin="6,0,0,0"
                                           TextTrimming="CharacterEllipsis"
                                           FontSize="{TemplateBinding FontSize}"
                                           FontWeight="{TemplateBinding FontWeight}"
                                           Foreground="{TemplateBinding Foreground}"
                                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SelectedCheck" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True"/>
                                    <Condition Property="IsHighlighted" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Compact selector uses the same selected-state palette as the wide segments. -->
        <Style x:Key="ActiveProfileMenuButtonStyle" TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="131*"/>
            <RowDefinition Height="161*"/>
        </Grid.RowDefinitions>
        <!-- Main Content with Blur Effect -->
        <Grid x:Name="MainContent" Grid.RowSpan="2">
            <Grid.Effect>
                <BlurEffect x:Name="BlurEffect" Radius="0"/>
            </Grid.Effect>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border x:Name="TitleBar" Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtBrandTitle" Text="TARKOV HELPER" FontSize="{DynamicResource FontSizeXLarge}" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>

                        <!-- Version display. Idle: a passive (non-clickable) chip with the current
                             version — manual checks live in Settings, auto-checks run every 3 min.
                             Update available: the chip swaps for a green "Update vX.Y.Z" pill whose
                             single action (install) is self-explanatory. -->
                        <Border x:Name="ChipVersion" Background="{StaticResource BackgroundLightBrush}"
                                CornerRadius="4" Padding="8,4" Margin="12,0,0,0"
                                VerticalAlignment="Center"
                                ToolTip="Current version — updates are checked automatically">
                            <TextBlock x:Name="TxtVersionChip" Text="v0.0.0"
                                       FontSize="{DynamicResource FontSizeTiny}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <Button x:Name="BtnVersionChip"
                                Padding="8,3" Margin="12,0,0,0"
                                Background="{StaticResource SuccessBrush}" Foreground="White"
                                Click="InstallUpdate_Click"
                                AutomationProperties.Name="Install update"
                                Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE896;"
                                           FontFamily="{StaticResource IconFont}"
                                           FontSize="{DynamicResource FontSizeTiny}"
                                           VerticalAlignment="Center" Margin="0,0,4,0"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                <TextBlock x:Name="TxtUpdatePillLabel" Text="Update v0.0.0"
                                           FontSize="{DynamicResource FontSizeTiny}" FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Right Side Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Sync / raid status chip: off / watching logs / matching / in raid.
                             A real Button (not a Border with a mouse handler) so it gets keyboard
                             focus, a UIA automation peer with InvokePattern, and press-capture
                             click semantics for free. -->
                        <Button x:Name="ChipSyncStatus" MinHeight="30" Padding="8,4" Margin="0,0,12,0"
                                VerticalAlignment="Center"
                                Click="ChipSyncStatus_Click"
                                AutomationProperties.Name="Sync status"
                                ToolTip="Game-log monitoring status — click to open Settings">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="SyncStatusDot" Width="8" Height="8"
                                         Fill="{StaticResource NeutralBrush}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtSyncStatus" Text="Sync off"
                                           FontSize="{DynamicResource FontSizeTiny}"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            </StackPanel>
                        </Button>

                        <!-- Wide profile selector: one-click, exact-one radio group. -->
                        <StackPanel x:Name="WideProfileSelector" Orientation="Horizontal"
                                    Margin="0,0,12,0" VerticalAlignment="Center"
                                    AutomationProperties.Name="Active profile">
                            <TextBlock x:Name="TxtActiveProfileLabel" Text="Active profile"
                                       FontSize="{DynamicResource FontSizeTiny}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Border MinHeight="30" Padding="1"
                                    Background="{StaticResource BackgroundLightBrush}"
                                    CornerRadius="4" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <!-- Content carries an EN default so the selector is
                                         readable from InitializeComponent onward; the
                                         localized label replaces it in UpdateAllLocalizedText.
                                         IsChecked is deliberately NOT set here: it would raise
                                         Checked during InitializeComponent, before the
                                         constructor builds _profileTransitionCueTimer, and the
                                         handler dereferences that timer. The constructor
                                         renders the initial selection instead. -->
                                    <RadioButton x:Name="BtnPvpZone" GroupName="ActiveProfile"
                                                 Content="PvP Zone"
                                                 Style="{StaticResource GameModeRadioStyle}"
                                                 Padding="7,3" FontSize="{DynamicResource FontSizeXSmall}"
                                                 FontWeight="SemiBold"
                                                 AutomationProperties.PositionInSet="1"
                                                 AutomationProperties.SizeOfSet="3"
                                                 Checked="BtnPvpZone_Checked"
                                                 Click="BtnPvpZone_Click"/>
                                    <RadioButton x:Name="BtnPveZone" GroupName="ActiveProfile"
                                                 Content="PvE Zone"
                                                 Style="{StaticResource GameModeRadioStyle}"
                                                 Padding="7,3" FontSize="{DynamicResource FontSizeXSmall}"
                                                 FontWeight="SemiBold"
                                                 AutomationProperties.PositionInSet="2"
                                                 AutomationProperties.SizeOfSet="3"
                                                 Checked="BtnPveZone_Checked"
                                                 Click="BtnPveZone_Click"/>
                                    <RadioButton x:Name="BtnPvpSeason" GroupName="ActiveProfile"
                                                 Content="PvP Season"
                                                 Style="{StaticResource GameModeRadioStyle}"
                                                 Padding="7,3" FontSize="{DynamicResource FontSizeXSmall}"
                                                 FontWeight="SemiBold"
                                                 AutomationProperties.PositionInSet="3"
                                                 AutomationProperties.SizeOfSet="3"
                                                 Checked="BtnPvpSeason_Checked"
                                                 Click="BtnPvpSeason_Click"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Compact/minimal selector: fixed-width trigger plus checked menu. -->
                        <!-- MinWidth, not Width: the label is localized and scales with the
                             user's BaseFontSize (10..28), so a hard 172 px cut the JA label off
                             from base 23 up. MinWidth keeps the stable default width the
                             fixed-marker-slot design wants while still growing when it must. -->
                        <Button x:Name="BtnActiveProfileMenu" MinWidth="172" MinHeight="30" Padding="8,3"
                                Style="{StaticResource ActiveProfileMenuButtonStyle}"
                                Margin="0,0,12,0" VerticalAlignment="Center"
                                Visibility="Collapsed" Click="BtnActiveProfileMenu_Click"
                                AutomationProperties.Name="Active profile">
                            <Button.ContextMenu>
                                <ContextMenu x:Name="ActiveProfileContextMenu" MinWidth="172"
                                             Style="{StaticResource ActiveProfileContextMenuStyle}"
                                             Placement="Bottom" VerticalOffset="2">
                                    <!-- EN Header defaults for the same reason as the wide
                                         RadioButtons above; UpdateAllLocalizedText replaces them. -->
                                    <MenuItem x:Name="MenuPvpZone" IsCheckable="True"
                                              Header="PvP Zone"
                                              Style="{StaticResource ActiveProfileMenuItemStyle}"
                                              Click="MenuPvpZone_Click"/>
                                    <MenuItem x:Name="MenuPveZone" IsCheckable="True"
                                              Header="PvE Zone"
                                              Style="{StaticResource ActiveProfileMenuItemStyle}"
                                              Click="MenuPveZone_Click"/>
                                    <MenuItem x:Name="MenuPvpSeason" IsCheckable="True"
                                              Header="PvP Season"
                                              Style="{StaticResource ActiveProfileMenuItemStyle}"
                                              Click="MenuPvpSeason_Click"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Width="16" Height="16" VerticalAlignment="Center">
                                    <Path x:Name="CompactProfileCheck"
                                          Data="M 2,8 L 6,12 L 14,3"
                                          Stroke="{StaticResource AccentBrush}"
                                          StrokeThickness="2.4" StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round" Stretch="Uniform"/>
                                    <Path x:Name="CompactProfileAutomatic" Width="13" Height="15"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Data="M 9,1 L 3,9 L 7,9 L 5,15 L 13,6 L 9,6 Z"
                                          Fill="{StaticResource AccentBrush}"
                                          Stretch="Uniform" Visibility="Hidden"/>
                                </Grid>
                                <TextBlock x:Name="TxtCompactActiveProfile" Grid.Column="1"
                                           Text="PvP Zone" Margin="6,0"
                                           TextTrimming="CharacterEllipsis"
                                           FontSize="{DynamicResource FontSizeXSmall}"
                                           FontWeight="SemiBold"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                           HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="&#xE70D;"
                                           FontFamily="{StaticResource IconFont}"
                                           FontSize="{DynamicResource FontSizeTiny}"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Button>

                        <!-- Visually hidden polite live region for automatic transitions. -->
                        <TextBlock x:Name="TxtProfileTransitionAnnouncement"
                                   Width="1" Height="1" Opacity="0"
                                   IsHitTestVisible="False"
                                   AutomationProperties.LiveSetting="Polite"/>

                        <!-- Profile chip: person glyph + level summary; opens the profile drawer -->
                        <Button x:Name="BtnProfile" MinHeight="30" Padding="9,4"
                                Margin="0,0,12,0" Click="BtnProfile_Click"
                                AutomationProperties.Name="Player profile"
                                ToolTip="Player profile — level, Scav Rep, DSP, edition, prestige">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE77B;" FontFamily="{StaticResource IconFont}"
                                           FontSize="{DynamicResource FontSizeXSmall}"
                                           VerticalAlignment="Center" Margin="0,0,6,0"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                <TextBlock x:Name="TxtProfileChipLevel" Text="Lv 15"
                                           FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                <TextBlock x:Name="TxtProfileChipChevron" Text="&#xE70D;"
                                           FontFamily="{StaticResource IconFont}"
                                           FontSize="{DynamicResource FontSizeTiny}"
                                           VerticalAlignment="Center" Margin="6,0,0,0"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            </StackPanel>
                        </Button>

                        <!-- Settings -->
                        <Button x:Name="BtnSettings" MinHeight="30" Padding="9,3"
                                Click="BtnSettings_Click"
                                AutomationProperties.Name="Settings"
                                ToolTip="Settings">
                            <TextBlock Text="&#xE713;" FontFamily="{StaticResource IconFont}"
                                       FontSize="{DynamicResource FontSizeMedium}"
                                       VerticalAlignment="Center"
                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Row="1">
                <!-- Welcome Text (shown when no data) -->
                <TextBlock x:Name="TxtWelcome" Text="Welcome to Tarkov Helper"
                           FontSize="{DynamicResource FontSizeHeader}" FontWeight="Light"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed"/>

                <!-- Tab Content Area -->
                <Grid x:Name="TabContentArea" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tab Navigation. Labels are localized in code (UpdateAllLocalizedText).
                         TabButtonStyle colors the checked tab by setting the RadioButton's
                         Foreground, which explicit TextBlocks do NOT inherit (the implicit
                         TextBlock style wins) — hence the scoped implicit style below, which
                         mirrors each tab's Foreground onto its glyph/label TextBlocks. -->
                    <Border x:Name="TabNavigation" Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" Padding="16,0">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Resources>
                                <!-- BasedOn is required: a bare implicit style would REPLACE the
                                     app-wide TextBlock style and drop AppFont from the
                                     labels (the glyphs set IconFont locally). Scoped to this
                                     panel so string-content controls elsewhere are unaffected. -->
                                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                    <Setter Property="Foreground"
                                            Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"/>
                                </Style>
                            </StackPanel.Resources>
                            <RadioButton x:Name="TabQuests"
                                         Style="{StaticResource TabButtonStyle}"
                                         IsChecked="True"
                                         AutomationProperties.Name="Quests"
                                         Checked="Tab_Checked">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="IcoTabQuests" Text="&#xE9D5;" FontFamily="{StaticResource IconFont}"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtTabQuests" Text="Quests" VerticalAlignment="Center"
                                               FontSize="{DynamicResource BaseFontSize}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="TabHideout"
                                         Style="{StaticResource TabButtonStyle}"
                                         AutomationProperties.Name="Hideout"
                                         Checked="Tab_Checked">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="IcoTabHideout" Text="&#xE80F;" FontFamily="{StaticResource IconFont}"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtTabHideout" Text="Hideout" VerticalAlignment="Center"
                                               FontSize="{DynamicResource BaseFontSize}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="TabItems"
                                         Style="{StaticResource TabButtonStyle}"
                                         AutomationProperties.Name="Items"
                                         Checked="Tab_Checked">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="IcoTabItems" Text="&#xE7B8;" FontFamily="{StaticResource IconFont}"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtTabItems" Text="Items" VerticalAlignment="Center"
                                               FontSize="{DynamicResource BaseFontSize}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="TabCollector"
                                         Style="{StaticResource TabButtonStyle}"
                                         AutomationProperties.Name="Collector"
                                         Checked="Tab_Checked">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="IcoTabCollector" Text="&#xE734;" FontFamily="{StaticResource IconFont}"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtTabCollector" Text="Collector" VerticalAlignment="Center"
                                               FontSize="{DynamicResource BaseFontSize}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </RadioButton>
                            <RadioButton x:Name="TabMap"
                                         Style="{StaticResource TabButtonStyle}"
                                         AutomationProperties.Name="Map"
                                         Checked="Tab_Checked">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="IcoTabMap" Text="&#xE707;" FontFamily="{StaticResource IconFont}"
                                               FontSize="{DynamicResource FontSizeSmall}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtTabMap" Text="Map" VerticalAlignment="Center"
                                               FontSize="{DynamicResource BaseFontSize}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </Border>

                    <!-- Tab Content -->
                    <ContentControl x:Name="PageContent" Grid.Row="1"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Profile Drawer Overlay (slides down from title bar).
             Top margin is set from TitleBar.ActualHeight in code so it tracks
             font-size changes instead of hardcoding the bar height. -->
        <Border x:Name="ProfileDrawer" Grid.RowSpan="2" Background="{StaticResource BackgroundLightBrush}"
                VerticalAlignment="Top"
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="16,12">
                <!-- Player Level Control (moved here from the title bar) -->
                <Border Background="{StaticResource BackgroundMediumBrush}"
                        CornerRadius="4" Padding="8,6" Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtProfileLevelLabel" Text="Level" FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button x:Name="BtnLevelDown" Content="-" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnLevelDown_Click"
                                ToolTip="Decrease level"/>
                        <TextBox x:Name="TxtPlayerLevel" Text="15" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 VerticalAlignment="Center" Margin="8,0"
                                 MinWidth="28" MaxWidth="40"
                                 TextAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 PreviewTextInput="TxtPlayerLevel_PreviewTextInput"
                                 LostFocus="TxtPlayerLevel_LostFocus"
                                 KeyDown="TxtPlayerLevel_KeyDown"/>
                        <Button x:Name="BtnLevelUp" Content="+" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnLevelUp_Click"
                                ToolTip="Increase level"/>
                    </StackPanel>
                </Border>

                <!-- Scav Rep Control -->
                <Border Background="{StaticResource BackgroundMediumBrush}"
                        CornerRadius="4" Padding="8,6" Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtScavRepLabel" Text="Scav Rep" FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button x:Name="BtnScavRepDown" Content="-" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnScavRepDown_Click"
                                ToolTip="Decrease Scav Rep (-0.1)"/>
                        <TextBox x:Name="TxtScavRep" Text="1.0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 VerticalAlignment="Center" Margin="8,0"
                                 MinWidth="36" MaxWidth="48"
                                 TextAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 PreviewTextInput="TxtScavRep_PreviewTextInput"
                                 LostFocus="TxtScavRep_LostFocus"
                                 KeyDown="TxtScavRep_KeyDown"/>
                        <Button x:Name="BtnScavRepUp" Content="+" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnScavRepUp_Click"
                                ToolTip="Increase Scav Rep (+0.1)"/>
                    </StackPanel>
                </Border>

                <!-- DSP Decode Count Control -->
                <Border Background="{StaticResource BackgroundMediumBrush}"
                        CornerRadius="4" Padding="8,6" Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtDspLabel" Text="DSP" FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button x:Name="BtnDsp0" Content="0" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnDsp_Click" Tag="0"
                                ToolTip="0 Decodes - No Make Amends quest"/>
                        <Button x:Name="BtnDsp1" Content="1" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnDsp_Click" Tag="1" Margin="2,0,0,0"
                                ToolTip="1 Decode - Make Amends: Buyout"/>
                        <Button x:Name="BtnDsp2" Content="2" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnDsp_Click" Tag="2" Margin="2,0,0,0"
                                ToolTip="2 Decodes - Make Amends: Security"/>
                        <Button x:Name="BtnDsp3" Content="3" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnDsp_Click" Tag="3" Margin="2,0,0,0"
                                ToolTip="3 Decodes - Make Amends: Software"/>
                    </StackPanel>
                </Border>

                <!-- Game Edition Checkboxes -->
                <Border Background="{StaticResource BackgroundMediumBrush}"
                        CornerRadius="4" Padding="8,6" Margin="0,0,16,0"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtEditionLabel" Text="Edition" FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <CheckBox x:Name="ChkEodEdition" Content="EOD"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  FontSize="{DynamicResource FontSizeXSmall}"
                                  VerticalAlignment="Center" Margin="0,0,12,0"
                                  Checked="ChkEdition_Changed" Unchecked="ChkEdition_Changed"
                                  ToolTip="Edge of Darkness Edition"/>
                        <CheckBox x:Name="ChkUnheardEdition" Content="Unheard"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  FontSize="{DynamicResource FontSizeXSmall}"
                                  VerticalAlignment="Center"
                                  Checked="ChkEdition_Changed" Unchecked="ChkEdition_Changed"
                                  ToolTip="The Unheard Edition"/>
                    </StackPanel>
                </Border>

                <!-- Prestige Level Control -->
                <Border Background="{StaticResource BackgroundMediumBrush}"
                        CornerRadius="4" Padding="8,6"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="TxtPrestigeLabel" Text="Prestige" FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button x:Name="BtnPrestigeDown" Content="-" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnPrestigeDown_Click"
                                ToolTip="Decrease prestige level"/>
                        <TextBlock x:Name="TxtPrestigeLevel" Text="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"
                                   VerticalAlignment="Center" Margin="8,0"
                                   MinWidth="16" TextAlignment="Center"/>
                        <Button x:Name="BtnPrestigeUp" Content="+" Width="24" Height="24"
                                Padding="0" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                Click="BtnPrestigeUp_Click"
                                ToolTip="Increase prestige level"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#AA1A1A1A" Grid.RowSpan="2">
            <Border Background="{StaticResource BackgroundDarkBrush}"
                    CornerRadius="8" Padding="32,24"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    MinWidth="320">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Loading Data" FontSize="{DynamicResource FontSizeLarge}" FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}"
                               HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <ProgressBar x:Name="LoadingProgressBar"
                                 Height="6" IsIndeterminate="True"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="{StaticResource BackgroundMediumBrush}"/>
                    <TextBlock x:Name="LoadingStatusText"
                               Text="Initializing..."
                               FontSize="{DynamicResource FontSizeXSmall}" Margin="0,12,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap" MaxWidth="280"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Settings Overlay -->
        <Grid x:Name="SettingsOverlay" Visibility="Collapsed" Background="#AA1A1A1A" Grid.RowSpan="2"
              MouseDown="SettingsOverlay_MouseDown">
            <Border Background="{StaticResource BackgroundDarkBrush}"
                    CornerRadius="8" Padding="24"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    MinWidth="800" MaxWidth="900" MaxHeight="550">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock x:Name="TxtSettingsTitle" Text="Settings" FontSize="{DynamicResource FontSizeTitle}" FontWeight="SemiBold"
                                   Foreground="{StaticResource AccentBrush}"/>
                        <Button x:Name="BtnCloseSettings" Content="✕"
                                HorizontalAlignment="Right"
                                Padding="8,4" FontSize="{DynamicResource FontSizeSmall}"
                                Click="BtnCloseSettings_Click"/>
                    </Grid>

                    <!-- Scrollable Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Column -->
                            <StackPanel Grid.Column="0">
                                <!-- Log Folder Section -->
                                <TextBlock x:Name="TxtLogFolderLabel" Text="Tarkov Log Folder"
                                           FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           Margin="0,0,0,8"/>

                                <TextBlock x:Name="TxtLogFolderDesc"
                                           Text="Set the path to Tarkov's Logs folder for automatic quest completion tracking."
                                           FontSize="{DynamicResource FontSizeXSmall}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,12"/>

                                <!-- Current Path Display -->
                                <Border Background="{StaticResource BackgroundMediumBrush}"
                                        CornerRadius="4" Padding="12,8" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="TxtCurrentLogPath"
                                                   Text="Not configured"
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock x:Name="TxtDetectionMethod"
                                                       Text=""
                                                       FontSize="{DynamicResource FontSizeTiny}"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Status Indicator -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                    <Ellipse x:Name="LogFolderStatusIndicator"
                                             Width="8" Height="8"
                                             Fill="#FF4444"
                                             VerticalAlignment="Center"
                                             Margin="0,0,8,0"/>
                                    <TextBlock x:Name="TxtLogFolderStatus"
                                               Text="Invalid path"
                                               FontSize="{DynamicResource FontSizeXSmall}"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="BtnAutoDetect" Content="Auto Detect"
                                            Padding="12,8" Margin="0,0,8,8"
                                            Click="BtnAutoDetect_Click"/>
                                    <Button x:Name="BtnBrowseLogFolder" Content="Browse..."
                                            Padding="12,8" Margin="0,0,8,8"
                                            Click="BtnBrowseLogFolder_Click"/>
                                    <Button x:Name="BtnResetLogFolder" Content="Reset"
                                            Padding="12,8" Margin="0,0,0,8"
                                            Click="BtnResetLogFolder_Click"/>
                                </WrapPanel>

                                <!-- Quest Sync Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtQuestSyncLabel" Text="Quest Log Sync"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>

                                        <TextBlock x:Name="TxtQuestSyncDesc"
                                                   Text="Synchronize quest progress from game log files. This will analyze your Tarkov logs and update completed quests."
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,12"/>

                                        <!-- Monitoring Status -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <Ellipse x:Name="MonitoringStatusIndicator"
                                                     Width="8" Height="8"
                                                     Fill="#FF4444"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0"/>
                                            <TextBlock x:Name="TxtMonitoringStatus"
                                                       Text="Not monitoring"
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>

                                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                            <Button x:Name="BtnSyncQuest" Content="Sync Quest"
                                                    Padding="12,8" Margin="0,0,8,8"
                                                    Click="BtnSyncQuest_Click"/>
                                            <Button x:Name="BtnToggleMonitoring" Content="Start Monitoring"
                                                    Padding="12,8" Margin="0,0,0,8"
                                                    Click="BtnToggleMonitoring_Click"/>
                                        </WrapPanel>

                                        <!-- Enter In-Progress Quests Button -->
                                        <Button x:Name="BtnInProgressQuestInput" Content="Enter In-Progress Quests"
                                                Padding="12,8" Margin="0,4,0,0"
                                                HorizontalAlignment="Left"
                                                Click="BtnInProgressQuestInput_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Application Update Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtSettingsUpdateLabel" Text="Application Update"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock x:Name="TxtSettingsVersion" Text="Current version: v0.0.0"
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock x:Name="TxtSettingsUpdateStatus" Text=""
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{StaticResource SuccessBrush}" Margin="8,0,0,0"/>
                                            <TextBlock x:Name="TxtSettingsLastCheck" Text=""
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0"/>
                                        </StackPanel>

                                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                            <Button x:Name="BtnCheckUpdateSettings"
                                                    Padding="12,8" Margin="0,0,8,8"
                                                    AutomationProperties.Name="Check for updates"
                                                    Click="BtnCheckUpdateSettings_Click">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}"
                                                               VerticalAlignment="Center" Margin="0,0,6,0"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                    <TextBlock x:Name="TxtSettingsCheckUpdateLabel" Text="Check for Updates"
                                                               VerticalAlignment="Center"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                </StackPanel>
                                            </Button>
                                            <Button x:Name="BtnUpdateAvailableSettings"
                                                    Padding="12,8" Margin="0,0,0,8"
                                                    Background="{StaticResource SuccessBrush}" Foreground="White"
                                                    AutomationProperties.Name="Install update"
                                                    Click="InstallUpdate_Click"
                                                    Visibility="Collapsed">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE896;" FontFamily="{StaticResource IconFont}"
                                                               VerticalAlignment="Center" Margin="0,0,6,0"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                    <TextBlock x:Name="TxtSettingsUpdateToLabel" Text="Update to v0.0.0"
                                                               FontWeight="SemiBold" VerticalAlignment="Center"
                                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                </StackPanel>
                                            </Button>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Right Column -->
                            <StackPanel Grid.Column="2">
                                <!-- Cache Management Section -->
                                <TextBlock Text="Cache Management"
                                           FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           Margin="0,0,0,8"/>

                                <TextBlock Text="Clear cached wiki pages and downloaded data. Use this if you encounter data issues or want to re-download everything."
                                           FontSize="{DynamicResource FontSizeXSmall}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,12"/>

                                <!-- Cache Size Display -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="Cache Size: "
                                               FontSize="{DynamicResource FontSizeXSmall}"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock x:Name="TxtCacheSize"
                                               Text="Calculating..."
                                               FontSize="{DynamicResource FontSizeXSmall}"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>

                                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button x:Name="BtnClearCache" Content="Clear Cache"
                                            Padding="12,8" Margin="0,0,8,8"
                                            Click="BtnClearCache_Click"/>
                                    <Button x:Name="BtnClearAllData" Content="Clear All Data"
                                            Padding="12,8" Margin="0,0,0,8"
                                            Click="BtnClearAllData_Click"
                                            ToolTip="Clear cache and all downloaded data (tasks, items, etc.)"/>
                                </WrapPanel>

                                <!-- Font Size Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Font Size"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>

                                        <TextBlock Text="Adjust the base font size for the application."
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,12"/>

                                        <!-- Font Size Control -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Button x:Name="BtnFontSizeDown" Content="-" Width="32" Height="32"
                                                    Grid.Column="0" Padding="0" FontSize="{DynamicResource FontSizeMedium}" FontWeight="Bold"
                                                    Click="BtnFontSizeDown_Click"/>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock x:Name="TxtCurrentFontSize" Text="16"
                                                           FontSize="{DynamicResource FontSizeLarge}" FontWeight="Bold"
                                                           Foreground="{StaticResource AccentBrush}"
                                                           VerticalAlignment="Center"/>
                                                <TextBlock Text=" pt"
                                                           FontSize="{DynamicResource FontSizeSmall}"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>

                                            <Button x:Name="BtnFontSizeUp" Content="+" Width="32" Height="32"
                                                    Grid.Column="2" Padding="0" FontSize="{DynamicResource FontSizeMedium}" FontWeight="Bold"
                                                    Click="BtnFontSizeUp_Click"/>
                                        </Grid>

                                        <Button x:Name="BtnResetFontSize" Content="Reset to Default"
                                                Padding="12,8" HorizontalAlignment="Left"
                                                Click="BtnResetFontSize_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Data Migration Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtDataMigrationLabel" Text="Data Migration"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>

                                        <TextBlock x:Name="TxtDataMigrationDesc"
                                                   Text="Import data from an older version of TarkovHelper (3.5 or earlier). Select the Config folder from your previous installation."
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,12"/>

                                        <Button x:Name="BtnDataMigration" Content="Import from Config..."
                                                Padding="12,8" HorizontalAlignment="Left"
                                                Click="BtnDataMigration_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Language Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtLanguageLabel" Text="Language"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>
                                        <ComboBox x:Name="CmbLanguage" Width="140" HorizontalAlignment="Left"
                                                  SelectionChanged="CmbLanguage_SelectionChanged">
                                            <ComboBoxItem Content="English" Tag="EN"/>
                                            <ComboBoxItem Content="한국어" Tag="KO"/>
                                            <ComboBoxItem Content="日本語" Tag="JA"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Border>

                                <!-- Support Section -->
                                <Border Style="{StaticResource SettingsSectionBorderStyle}">
                                    <StackPanel>
                                        <TextBlock x:Name="TxtSupportLabel" Text="Support"
                                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   Margin="0,0,0,8"/>
                                        <TextBlock x:Name="TxtSupportDesc"
                                                   Text="If Tarkov Helper is useful to you, you can support its development."
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,12"/>
                                        <Button x:Name="BtnCoffee" Padding="12,8" HorizontalAlignment="Left"
                                                AutomationProperties.Name="Buy me a coffee"
                                                Click="BtnCoffee_Click">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xEB51;" FontFamily="{StaticResource IconFont}"
                                                           VerticalAlignment="Center" Margin="0,0,6,0"
                                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                <TextBlock x:Name="TxtSupportButtonLabel" Text="Buy me a coffee"
                                                           VerticalAlignment="Center"
                                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <!-- Danger Zone Section -->
                                <Border BorderBrush="#55F44336" BorderThickness="1" CornerRadius="4"
                                        Margin="0,16,0,0" Padding="12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <TextBlock Text="&#xE7BA;" FontFamily="{StaticResource IconFont}"
                                                       FontSize="{DynamicResource FontSizeSmall}"
                                                       Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                            <TextBlock x:Name="TxtDangerZoneLabel" Text="Danger Zone"
                                                       FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                                       Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="TxtResetProgressDesc"
                                                   Text="Reset all quest and hideout progress. This cannot be undone."
                                                   FontSize="{DynamicResource FontSizeXSmall}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" Margin="0,0,0,12"/>
                                        <Button x:Name="BtnResetProgress" Content="Reset All Progress"
                                                Padding="12,8" HorizontalAlignment="Left"
                                                Background="Transparent" BorderBrush="{StaticResource ErrorBrush}" Foreground="{StaticResource ErrorBrush}"
                                                Click="BtnResetProgress_Click"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

    </Grid>

</Window>
