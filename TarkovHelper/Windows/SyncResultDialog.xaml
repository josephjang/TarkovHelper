<Window x:Class="TarkovHelper.Windows.SyncResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quest Sync Result"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundDarkBrush}"
            CornerRadius="8" Padding="24"
            MinWidth="560" MaxWidth="720" MaxHeight="650">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock x:Name="TxtTitle" Text="Quest Sync Complete" FontSize="{DynamicResource FontSizeXLarge}" FontWeight="SemiBold"
                           Foreground="{DynamicResource AccentBrush}"/>
                <Button x:Name="BtnClose" Content="X"
                        HorizontalAlignment="Right"
                        Padding="8,4" FontSize="{DynamicResource FontSizeSmall}"
                        Click="BtnClose_Click"/>
            </Grid>

            <!--
                Applied-per-profile summary. This replaced the per-quest confirmation list:
                once attribution comes from the logs rather than from the selection, reviewing
                quest names one by one asks the player to confirm a judgment they have no better
                information about than the app does, over a list that now spans several
                profiles. The summary naming the profiles written to is what a player has to
                notice a sync went somewhere unexpected (PRD R2).
            -->
            <Border Grid.Row="1" Background="{DynamicResource BackgroundMediumBrush}"
                    CornerRadius="4" Margin="0,0,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{DynamicResource BackgroundLightBrush}"
                            CornerRadius="4,4,0,0" Padding="12,8">
                        <TextBlock x:Name="TxtAppliedHeader" Text="Applied by profile"
                                   FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="180">
                        <ItemsControl x:Name="AppliedByProfileList"
                                      AutomationProperties.AutomationId="LstSyncAppliedByProfile">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}"
                                            Padding="12,6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding ProfileName}"
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding AppliedText}"
                                                       FontSize="{DynamicResource FontSizeXSmall}"
                                                       Foreground="{DynamicResource AccentBrush}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}"
                            Padding="12,8">
                        <TextBlock x:Name="TxtStats"
                                   Text=""
                                   FontSize="{DynamicResource FontSizeTiny}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Alternative Quests Selection (PRD R2a: the only choices the logs cannot answer) -->
            <Border x:Name="AlternativeQuestsSection" Grid.Row="2"
                    Background="{DynamicResource BackgroundMediumBrush}"
                    CornerRadius="4" Margin="0,0,0,12"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#44FFA726"
                            CornerRadius="4,4,0,0" Padding="12,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="" FontSize="{DynamicResource FontSizeSmall}"
                                       Foreground="#FFA726" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtAlternativeHeader"
                                       Text="Optional Quests - Choose One Per Group"
                                       FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="180">
                        <ItemsControl x:Name="AlternativeQuestsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderBrush}"
                                            Padding="12,8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding GroupLabel}"
                                                       FontSize="{DynamicResource FontSizeTiny}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       Margin="0,0,0,6"
                                                       TextWrapping="Wrap"/>
                                            <ItemsControl ItemsSource="{Binding Choices}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <RadioButton GroupName="{Binding GroupName}"
                                                                     IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                     Margin="0,0,16,0"
                                                                     IsEnabled="{Binding IsEnabled}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding QuestName}"
                                                                           FontSize="{DynamicResource FontSizeXSmall}"
                                                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                                <TextBlock Text=" (Done)"
                                                                           FontSize="{DynamicResource FontSizeTiny}"
                                                                           Foreground="{DynamicResource AccentBrush}"
                                                                           Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                                <TextBlock Text=" (Failed)"
                                                                           FontSize="{DynamicResource FontSizeTiny}"
                                                                           Foreground="#EF5350"
                                                                           Visibility="{Binding IsFailed, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                            </StackPanel>
                                                        </RadioButton>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Skip"
                        Padding="16,8" Margin="0,0,8,0"
                        Visibility="Collapsed"
                        Click="BtnCancel_Click"/>
                <Button x:Name="BtnConfirm" Content="OK"
                        Padding="16,8"
                        Click="BtnConfirm_Click"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="{DynamicResource BackgroundDarkBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
